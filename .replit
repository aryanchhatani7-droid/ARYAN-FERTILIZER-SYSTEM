[run]
command = "cd backend && uvicorn app.main:app --host 0.0.0.0 --port $PORT --reload"
language = "nix"
icon = "python"

[[secret]]
name = "DATABASE_URL"
description = "PostgreSQL database connection"

[[secret]]
name = "JWT_SECRET"
description = "Secret key for security tokens"
